/**
 * ============================================================================
 * PATHOS DESKTOP WORKBENCH ROUTE (Day 48)
 * ============================================================================
 */

import { PathosWorkbenchWorkspace } from '@/components/workbench/PathosWorkbenchWorkspace';

export default function WorkbenchPage() {
  return <PathosWorkbenchWorkspace />;
}
