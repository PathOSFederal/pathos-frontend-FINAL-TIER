{
  "name": "@pathos/core",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types, stores, and utilities for PathOS — no next/* or electron/* imports allowed.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.5",
    "zustand": "^5.0.9"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
